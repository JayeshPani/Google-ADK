# Shared UI Components

This repository starts as a Streamlit application, so shared UI primitives are implemented through CSS classes and helper render functions instead of React components.

Primary UI source:

```python
# app/streamlit_app.py
# Source code is generated later in this repository and should be treated as the current UI surface.
```

